---
import { getImageUrl } from "../js/backend.mjs";
import { Image } from "astro:assets";

const { record, imageField } = Astro.props;

const imageURL = await getImageUrl(record, imageField);
---

{
    imageURL && (
        <Image
            src={imageURL}
            alt={record.nom_evenement || "Image"}
            inferSize={false}
            width={300}
            height={300}
        />
    )
}
